# Sway output configuration
# Run 'swaymsg -t get_outputs' to verify output names if these don't work
# X11 DisplayPort-0 → likely DP-1, DisplayPort-1 → likely DP-2 (verify!)

output DP-2 mode 1920x1080@240Hz position 0,0
output DP-1 mode 1920x1080@144Hz position 1920,0

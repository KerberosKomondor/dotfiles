name: work
root: ~/code

windows:
  - code:
      root: ~/code
      panes:
        - ~/res/tmuxinator_init.sh
  - claude:
      root: ~/code
  - frontend:
      root: ~/code
  - storybook:
      root: ~/code

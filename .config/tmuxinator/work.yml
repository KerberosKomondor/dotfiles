name: work
root: ~/code

windows:
  - Choice360:
      root: ~/code
      panes:
        - ~/res/tmuxinator_init.sh
  - claude:
      root: ~/code
  - frontend:
      root: ~/code
  - storybook:
      root: ~/code

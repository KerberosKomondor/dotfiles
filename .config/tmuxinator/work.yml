name: work
root: ~/code

windows:
  - Choice360:
      root: ~/code
  - claude:
      root: ~/code
  - frontend:
      root: ~/code
  - storybook:
      root: ~/code
      panes:
        - tmux command-prompt -p "Project name:" "run-shell '~/res/change_project_dir.sh %%'"
